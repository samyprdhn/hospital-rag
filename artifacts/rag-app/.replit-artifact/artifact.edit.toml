kind = "design"
previewPath = "/"
title = "RAG Document QA"
version = "1.0.0"
id = "rag-doc-qa-001"

[[services]]
localPort = 5000
name = "RAG App"
paths = ["/"]

[services.development]
run = "cd artifacts/rag-app && uvicorn main:app --host 0.0.0.0 --port 5000 --reload"
